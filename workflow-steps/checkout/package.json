{
  "name": "checkout-step",
  "private": true,
  "main": "output/main.js",
  "scripts": {
    "build": "pnpm exec esbuild main.ts --bundle --platform=node --format=cjs --target=node20 --outfile=output/main.js",
    "test": "jest",
    "test:coverage": "jest --coverage"
  }
}
